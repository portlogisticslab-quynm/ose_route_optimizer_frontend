import {useState} from 'react';import {useAuth} from '../context/AuthContext'
export default function Login(){const {login}=useAuth();const [id,setId]=useState('ADMIN'),[pw,setPw]=useState('admin123'),[err,setErr]=useState('');return <div className="login-page"><div className="login-card"><div className="brand-row"><div className="logo">OSE</div><div><h1>Multimodal Route Optimizer</h1><p>V.1.1.1 EDU - Copyright 2026 portlogistics.vn</p></div></div><label>User ID</label><input value={id} onChange={e=>setId(e.target.value)}/><label>Password</label><input type="password" value={pw} onChange={e=>setPw(e.target.value)}/><button onClick={async()=>{try{setErr('');await login(id,pw)}catch(e:any){setErr(e.message)}}}>Login</button>{err&&<div className="error">{err}</div>}<div className="note">First run: ADMIN / admin123. Change it before classroom deployment.</div></div></div>}
